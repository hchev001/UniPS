/**
 * Provides model for Address object
 */

export class Address {
    constructor (
        line1: string,
        line2: string,
        city: string,
        state: string,
        zip: number
    ) {}
}
