/**
 * Provides model for Address object
 */

export class Address {
    constructor (
        city: string,
        line1: string,
        line2: string,
        state: string,
        zip: number
    ) {}
}
